import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 20,
    fontWeight: '300',
  },
  mainView: {
    flex: 1,
  },
});

export default styles;
