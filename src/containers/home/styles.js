import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  spashImage: {
    flex: 1,
    width: null,
    height: null,
    resizeMode: 'cover',
  },
});

export default styles;
